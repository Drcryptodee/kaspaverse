# Build artifacts — anchored: an unanchored build/ swallowed the unrelated
# .claude/skills/build/ directory (L24)
/build/
android/**/build/
rust/target/
.dart_tool/
.gradle/
*.apk
*.aab
dist/

# Generated bindings (regenerate via flutter_rust_bridge_codegen)
# lib/src/rust/ is committed for reviewability — never hand-edited

# Secrets & local environment — NEVER committed (INV-3, INV-11)
*.keystore
*.jks
key.properties
docs/environment.local.md
.env*

# IDE / OS
.idea/
.vscode/
*.iml
.DS_Store

# Flutter
.flutter-plugins
.flutter-plugins-dependencies
.packages
pubspec.lock.bak

# Personal AI-workflow — local only, versioned in the private ops mirror (D-047)
CLAUDE.md
.claude/
docs/sessions/
docs/audits/
docs/proposals/
docs/phases/
docs/COMPLETION_HISTORY.md
docs/REAUDIT_CAMPAIGN_PROMPT.md
docs/CLAUDE_DESIGN_PROMPT.md
docs/FINAL_GROUNDING_AUDIT_PROMPT.md

[package]
name = "kaspaverse-core"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
kaspa-bip32.workspace = true
kaspa-wallet-keys.workspace = true
kaspa-wallet-core.workspace = true
kaspa-consensus-core.workspace = true
kaspa-addresses.workspace = true
argon2.workspace = true
chacha20poly1305.workspace = true
zeroize.workspace = true
secp256k1.workspace = true
sha2.workspace = true
hmac.workspace = true

[dev-dependencies]
# Parity-fixture harness only (tests/transport_parity.rs) — already resolved
# in the lock via the pinned tree; never compiled into the shipped library.
serde_json = "1.0"

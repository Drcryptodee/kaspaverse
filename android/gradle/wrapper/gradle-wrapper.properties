distributionBase=GRADLE_USER_HOME
distributionPath=wrapper/dists
# INV-7. The gate's `kotlin compile` lane executes this distribution, so its
# integrity is verified rather than assumed. Published value, cross-checked
# against services.gradle.org and the local cache (re-verified 2026-08-16: the
# cached gradle-8.14-all.zip hashes to exactly this).
#
# The jar that ENFORCES this line is itself pinned now. Until 2026-08-16 the
# wrapper jar here was Gradle 2.10 — authentic (it matches Gradle's published
# gradle-2.10-wrapper.jar.sha256) but a 2015 bootstrapper nobody chose, left
# behind by the Flutter scaffold while the distribution moved to 8.14. It did
# honour this property, so the pin was live; it was simply un-owned. It is now
# the official 8.14 wrapper, asserted by the gate's `gradle wrapper jar` lane
# against the same checksum Gradle publishes (product-audit run 2, F2).
distributionSha256Sum=efe9a3d147d948d7528a9887fa35abcf24ca1a43ad06439996490f77569b02d1
distributionUrl=https\://services.gradle.org/distributions/gradle-8.14-all.zip
networkTimeout=10000
validateDistributionUrl=true
zipStoreBase=GRADLE_USER_HOME
zipStorePath=wrapper/dists
